function main(){
  const colors = ["verde", "amarelo", "azul", "rosa", "laranja"]

  // Pega conforme a ordem do array
  const [firstColor, secondColor] = colors;

  console.log(firstColor, secondColor)
}
main()